haystack-ai>=2.10.0                # For building LLM pipelines
streamlit                         # Web app framework
requests                          # To make HTTP requests (e.g., Serper.dev)
beautifulsoup4                    # For HTML parsing
python-dotenv                     # To load .env secrets
google-generativeai              # For using Gemini LLMs
protobuf<5.0.0                    # Ensures compatibility with grpc dependencies
tqdm                              # Optional but used in many pipelines
